// This file tells Jenkins what to do when code is pushed
pipeline {
  agent { label 'jenkins-slave' }
  stages {
    stage('Placeholder') {
      steps {
        echo 'Jenkins pipeline connected!'
      }
    }
  }
}
